[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["motools", "mozoo"]

[project]
name = "motools"
version = "0.1.0"
description = "Infrastructure for training and evaluating model organisms"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "inspect-ai>=0.3.0",
    "loguru>=0.7.0",
    "pandas>=2.0.0",
    "aiofiles>=23.0.0",
    "python-dotenv>=1.0.0",
    "datasets>=3.5.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "scipy>=1.10.0",
    "matplotlib>=3.8.0",
    "tinker>=0.2.2",
    "transformers>=4.40.0",
    "skypilot>=0.10.0",
    "networkx>=3.0",
    "filelock>=3.10.0",
    "aiosqlite>=0.20.0",
    "plotly>=5.0.0",
    "mashumaro[yaml]>=3.10",
    "kaleido>=0.2.0",
    "cloudpickle>=3.0.0",
    "openweights>=0.7.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]  # Ignore line length in string literals/templates

[dependency-groups]
dev = [
    "hatchling>=1.27.0",
    "myst-parser>=4.0.0",
    "pandas-stubs>=2.3.2.250926",
    "pytest>=8.4.2",
    "pytest-asyncio>=1.2.0",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.15.1",
    "respx>=0.22.0",
    "ruff>=0.14.0",
    "sphinx>=8.2.3",
    "sphinx-autodoc-typehints>=3.5.2",
    "sphinx-rtd-theme>=3.0.2",
    "types-aiofiles>=25.1.0.20251011",
    "types-networkx>=3.5.0.20251001",
    "types-pyyaml>=6.0.12.20250915",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
    "slow: marks tests as slow running (deselect with '-m \"not slow\"')",
]
filterwarnings = [
    "ignore:String-based task references are deprecated:DeprecationWarning",
    "ignore:Unrecognized atom_type:UserWarning",
]

[project.scripts]
lint = "scripts.lint:main"
motools = "motools.cli.main:main"
