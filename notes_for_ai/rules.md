design.md contains the relevant high-level architectural details.
Always use `uv add` to add dependencies. 
If you need to record something for future use, do it in a file under `notes_for_ai/running_notes.md`

When I say "test", you should run all unit tests and iteratively fix issues until the tests pass. 

Whan I say "lint", you should fix all style issues / ensure linters pass. 

When I say "commit", you should git commit the current changes with an informative commit message. 